import React from 'react';
import { Header } from 'semantic-ui-react';

export default class MemorizeTiles extends React.Component {
  render() {
    return (
      <Header>Memorize Tiles</Header>
    );
  }
}